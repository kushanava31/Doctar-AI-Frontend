/** DOCTAR's canonical customer-care / helpline number.
 * Matches the number the backend already quotes in its FAQ replies
 * (backend-node/src/services/chatData.ts, chat.ts) — kept in sync manually
 * since the frontend and backend are separate repos/deployments with no
 * shared code. This is the frontend's single source of truth for it. */
export const DOCTAR_HELPLINE = "8877772277";
